{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pnpm -w install --frozen-lockfile",
  "buildCommand": "pnpm --filter @scaler/types build && pnpm build",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": false
  }
}
