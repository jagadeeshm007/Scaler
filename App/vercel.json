{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd .. && pnpm install --frozen-lockfile --ignore-scripts",
  "buildCommand": "pnpm --filter @bolt/types build && pnpm build",
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": false
  }
}
