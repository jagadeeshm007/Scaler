/** Browser-only API surface — use in client components and hooks. */
export { api, axiosInstance, localApi, localAxios } from '@/lib/api/client';
export { ApiError, getErrorMessage } from '@/lib/api/errors';
