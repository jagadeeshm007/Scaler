{
  "name": "scaler",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd server && pnpm dev",
    "dev:frontend": "cd App && pnpm dev",
    "lint": "pnpm --filter scaler-app lint && pnpm --filter server lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "pnpm --filter @bolt/types typecheck && pnpm --filter server typecheck && pnpm --filter scaler-app typecheck",
    "build": "pnpm --filter @bolt/types build && pnpm --filter server build && pnpm --filter scaler-app build",
    "build:types": "pnpm --filter @bolt/types build",
    "prepare": "husky"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.61.0",
    "@typescript-eslint/parser": "^8.61.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "husky": "^9.1.7",
    "prettier": "^3.2.5"
  },
  "overrides": {
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3"
  }
}
